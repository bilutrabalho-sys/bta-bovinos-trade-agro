# Conexão com o PostgreSQL do BTA.
# Local (dev): banco "bta" no Postgres da sua máquina, usuário dono/superuser.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/bta

# Porta do servidor HTTP.
PORT=3001

# --- Autenticação de usuário (login próprio: e-mail + senha + JWT) ---------
# Segredo que ASSINA os tokens JWT de sessão dos usuários. OBRIGATÓRIO: a API
# NÃO sobe sem ele (fail-closed). Use um valor longo e aleatório (>= 32 chars).
# Gere um assim:  node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
# NUNCA versione o valor real (este arquivo é só o template) e use um segredo
# DIFERENTE por ambiente (dev/prod). Trocar o segredo invalida todos os tokens.
JWT_SECRET=troque-por-um-segredo-longo-e-aleatorio

# --- Painel administrativo de preços (uso interno) -------------------------
# Token que protege as rotas /api/admin/market/* e a página /admin.
# - Se DEIXAR EM BRANCO / não definir: o painel admin fica DESABILITADO
#   (as rotas respondem 503). É o padrão seguro — nunca abre por engano.
# - Para habilitar, gere um token longo e aleatório e coloque aqui. Ex.:
#   ADMIN_TOKEN=troque-por-um-token-longo-e-aleatorio
# NUNCA versione o valor real (este arquivo é só o template).
ADMIN_TOKEN=

# Origens permitidas de CORS em PRODUÇÃO (lista separada por vírgula).
# - Se DEIXAR EM BRANCO / não definir: CORS liberado (qualquer origem) — ótimo
#   para dev, o Vite em localhost:5173 funciona sem configurar nada.
# - Em produção, preencha com as origens reais do app para travar o CORS. Ex.:
# ALLOWED_ORIGINS=https://app.bta.com.br,https://www.bta.com.br

# --- Nuvem -----------------------------------------------------------------
# Para apontar para um Postgres gerenciado (Neon, Supabase, RDS, Railway...),
# basta trocar a DATABASE_URL. A maioria exige SSL — acrescente ?sslmode=require
# ao final (o Neon já inclui isso na string que ele te dá). Ex.:
# DATABASE_URL=postgres://usuario:senha@host/banco?sslmode=require
